const connection = require('../connection');

export default class{
    addClass({ classname,teacherid, major}, callback){

    }

    deleteClass({classid}, callback){

    }

    updateClass({classid, classname, teacherid, major}, callback){

    }

    getClassList({search, sortby, sort, index, rows}, callback){
        
    }
}